@using PortfolioEngine.Domain.Configs

<div id="techModal" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900/60 backdrop-blur-sm opacity-0 pointer-events-none transition-opacity duration-200">
    <div id="techModalContainer" class="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-2xl w-full max-w-lg max-h-full flex flex-col transform scale-95 transition-transform duration-200 overflow-hidden">

        <!-- Sticky Header -->
        <div class="px-6 py-4 border-b border-slate-100 dark:border-slate-800 flex items-center justify-between shrink-0">
            <h3 id="techModalTitle" class="text-lg font-bold text-slate-900 dark:text-white flex items-center gap-2">
                <i id="techModalIcon" class="bi bi-cpu text-blue-600"></i>
                <span id="techModalTitleText">Add New Technology</span>
            </h3>
            <button type="button" onclick="closeTechModal()" class="text-slate-400 hover:text-slate-600 p-1 rounded-lg">
                <i class="bi bi-x-lg text-lg"></i>
            </button>
        </div>

        <!-- Form Wrapper -->
        <form id="techForm" method="post" class="flex flex-col min-h-0 flex-1">
            @Html.AntiForgeryToken()

            <!-- Hidden field for Technology Id -->
            <input type="hidden" id="tech_Id" name="Id" value="" />

            <!-- Scrollable Body -->
            <div class="p-6 space-y-4 overflow-y-auto flex-1 custom-scrollbar">

                <!-- Validation Summary -->
                <div id="techValidationSummary" class="hidden p-3 bg-red-50 dark:bg-red-950/40 border border-red-200 dark:border-red-800 rounded-lg text-xs text-red-600 dark:text-red-400"></div>

                <!-- Parent Category -->
                <div>
                    <label class="block text-xs font-semibold uppercase tracking-wider text-slate-700 dark:text-slate-300 mb-1">Parent Category</label>
                    <select id="tech_CategoryId" name="TechCategoryId" asp-items="ViewBag.Categories" required
                            class="w-full px-3.5 py-2 rounded-xl bg-slate-50 dark:bg-slate-800/60 border border-slate-200 dark:border-slate-700 text-sm text-slate-900 dark:text-white focus:ring-2 focus:ring-blue-500">
                        <option value="">-- Select Category --</option>
                    </select>
                </div>

                <!-- Technology Name -->
                <div>
                    <label class="block text-xs font-semibold uppercase tracking-wider text-slate-700 dark:text-slate-300 mb-1">Technology Name</label>
                    <input id="tech_Name" name="Name" required placeholder="e.g. C#, TanStack, Tailwind"
                           class="w-full px-3.5 py-2 rounded-xl bg-slate-50 dark:bg-slate-800/60 border border-slate-200 dark:border-slate-700 text-sm text-slate-900 dark:text-white focus:ring-2 focus:ring-blue-500" />
                </div>

                <!-- Icon Picker Grid -->
                <div>
                    <div class="flex items-center justify-between mb-1">
                        <label class="block text-xs font-semibold uppercase tracking-wider text-slate-700 dark:text-slate-300">Select Icon</label>
                        <span class="text-xs font-medium text-blue-600 dark:text-blue-400 flex items-center gap-1.5">
                            <i id="selectedIconPreview" class="bi bi-code-slash text-sm"></i>
                            <span id="selectedIconName">code-slash</span>
                        </span>
                    </div>

                    <!-- Hidden Input for IconClass -->
                    <input type="hidden" id="tech_IconClass" name="IconClass" value="code-slash" />

                    <!-- Search Input -->
                    <div class="relative mb-2">
                        <i class="bi bi-search absolute left-3 top-2.5 text-xs text-slate-400"></i>
                        <input type="text" id="iconSearchInput" placeholder="Search icons..."
                               class="w-full pl-8 pr-3 py-1.5 rounded-lg bg-slate-50 dark:bg-slate-800/60 border border-slate-200 dark:border-slate-700 text-xs text-slate-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all" />
                    </div>

                    <!-- 5-Column Grid -->
                    <div class="h-44 overflow-y-auto p-2 bg-slate-50 dark:bg-slate-800/40 rounded-xl border border-slate-200 dark:border-slate-700/80 custom-scrollbar">
                        <div class="grid grid-cols-5 gap-2" id="iconGrid">
                            @foreach (var icon in IconConfig.AllowedIcons)
                            {
                                <button type="button"
                                        data-icon-key="@icon.Key"
                                        data-icon-label="@icon.Label"
                                        title="@icon.Label (@icon.Key)"
                                        class="icon-option-btn group flex flex-col items-center justify-center p-2.5 rounded-xl border border-slate-200/60 dark:border-slate-700/50 bg-white dark:bg-slate-800 text-slate-600 dark:text-slate-300 hover:border-blue-500 dark:hover:border-blue-500 hover:text-blue-600 dark:hover:text-blue-400 hover:shadow-sm transition-all">
                                    <i class="bi bi-@icon.Key text-xl transition-transform group-hover:scale-110"></i>
                                </button>
                            }
                        </div>
                    </div>
                </div>

                <!-- Description -->
                <div>
                    <label class="block text-xs font-semibold uppercase tracking-wider text-slate-700 dark:text-slate-300 mb-1">Description</label>
                    <textarea id="tech_Description" name="Description" rows="3" required placeholder="Short summary of this technology..."
                              class="w-full px-3.5 py-2 rounded-xl bg-slate-50 dark:bg-slate-800/60 border border-slate-200 dark:border-slate-700 text-sm text-slate-900 dark:text-white focus:ring-2 focus:ring-blue-500"></textarea>
                </div>
            </div>

            <!-- Sticky Footer Actions -->
            <div class="px-6 py-4 flex items-center justify-end gap-3 border-t border-slate-100 dark:border-slate-800 shrink-0 bg-white dark:bg-slate-900">
                <button type="button" onclick="closeTechModal()" class="px-4 py-2 text-sm font-medium text-slate-600 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-xl">Cancel</button>
                <button type="submit" id="techSubmitBtn" class="px-5 py-2 text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 rounded-xl shadow-sm">Save Technology</button>
            </div>
        </form>
    </div>
</div>